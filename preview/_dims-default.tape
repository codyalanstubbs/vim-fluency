# Default terminal size for the single-buffer previews (training, lesson,
# end screen). Source after _setup.tape, before _launch.tape. Dimensions
# live in a fragment because VHS keeps the FIRST Set per key, so _setup
# can't set them if scenes need to override.
Set FontSize 20
Set Width 1100
Set Height 680
